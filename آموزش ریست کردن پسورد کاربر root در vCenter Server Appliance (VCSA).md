# 🔧 آموزش ریست کردن پسورد کاربر root در vCenter Server Appliance (VCSA)

اگر رمز عبور کاربر **root** در vCenter را فراموش کرده‌اید، می‌توانید با دسترسی به کنسول مستقیم و از طریق **GRUB bootloader**، پسورد را ریست کرده و دوباره دسترسی پیدا کنید.

> ⚠️ **توجه:** این عملیات نیاز به دسترسی به کنسول مستقیم (iLO، iDRAC، IPMI یا کنسول ESXi/vSphere) دارد و باعث ریبوت شدن vCenter خواهد شد. بهتر است در زمان‌های نگهداری (maintenance window) انجام شود.

---

## 🖥 پیش‌نیازها

- دسترسی به کنسول ماشین مجازی vCenter
- ماشین باید روشن باشد
- قابل استفاده برای نسخه‌های: 6.5، 6.7، 7.x و 8.x

---

## 🔄 مراحل ریست پسورد root

### 1. ریستارت کردن vCenter

از طریق کنسول (نه SSH)، ماشین مجازی vCenter را ریبوت (restart) کنید.

### 2. ورود به محیط GRUB

در هنگام بوت سیستم:
- زمانی که صفحه **GRUB** ظاهر شد، سریعاً یک کلید (مثلاً جهت بالا یا پایین) فشار دهید تا شمارش معکوس متوقف شود.
- گزینه‌ای که با `Photon` شروع می‌شود را انتخاب کنید (گزینه پیش‌فرض).
- سپس کلید **`e`** را بزنید تا وارد حالت ویرایش شویم.

### 3. ویرایش پارامترهای بوت
https://knowledge.broadcom.com/external/article/322247/resetting-root-password-in-vcenter-serve.html
در صفحه ویرایش:
- خطی که با `linux` یا `linuxefi` شروع می‌شود را پیدا کنید.
- در انتهای آن، عبارت زیر را اضافه کنید:
  ```bash
  rw init=/bin/bash
  ```
- حالا کلید **F10** یا ترکیب `Ctrl + X` را بزنید تا سیستم با این تنظیمات بوت شود.

### 4. فعال‌سازی حالت نوشتن روی فایل‌سیستم (در صورت نیاز)

در محیط bash که ظاهر می‌شود، دستور زیر را وارد کنید:
```bash
mount -o remount,rw /
```

### 5. ریست کردن پسورد

حالا با این دستور رمز عبور جدید را تنظیم کنید:
```bash
passwd
```

رمز جدید را وارد کرده و مجدد تأیید کنید.

### 6. راه‌اندازی مجدد سیستم

پس از تنظیم موفق رمز عبور، با دستور زیر سیستم را راه‌اندازی مجدد کنید:
```bash
exec /sbin/init
```

---

## ✅ پس از ریست پسورد

اکنون می‌توانید با رمز عبور جدید از طریق SSH یا کنسول وارد شوید.  
در صورتی که حساب root قفل یا منقضی شده باشد، می‌توانید با دستورات زیر آن را فعال کنید:

```bash
chage -l root
chage -E -1 root
```

---

## 🔒 توصیه‌های امنیتی

- پس از بازیابی دسترسی، رمز را در یک ابزار مدیریت رمز عبور ذخیره کنید.
- در صورت فعال بودن expiration برای پسورد، زمان انقضا را یادداشت کرده یا هشدار تنظیم کنید.
- بررسی لاگ‌ها برای لاگین‌های ناموفق و فعال‌سازی هشدارهای امنیتی پیشنهاد می‌شود.

> 📝 این روش به‌صورت رسمی توسط VMware برای نسخه‌های appliance-based vCenter (VCSA) پشتیبانی می‌شود و فقط در شرایط اضطراری استفاده گردد.
