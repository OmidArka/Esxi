# 🔐 بازیابی رمز عبور حساب Administrator در vCenter

در صورتی که رمز عبور اکانت **Administrator@vsphere.local** را در vCenter فراموش کرده‌اید، نگران نباشید! در صورتی که به کنسول اصلی vCenter دسترسی داشته باشید و رمز عبور کاربر `root` را نیز بدانید، می‌توانید به‌راحتی رمز عبور را ریست کرده و دسترسی خود را بازیابی کنید.

## پیش‌نیازها
- دسترسی مستقیم به کنسول vCenter (به‌صورت فیزیکی یا از طریق iLO/IPMI/DRAC)
- دسترسی به حساب کاربری `root` در vCenter

---

## مراحل بازیابی رمز عبور

### 1. اتصال به vCenter با SSH
ابتدا نرم‌افزار [PuTTY](https://www.putty.org) را دانلود و نصب کنید. سپس مراحل زیر را دنبال نمایید:

- برنامه PuTTY را اجرا کنید.
- در بخش **Host Name (or IP address)**، آدرس IP یا نام میزبان vCenter را وارد نمایید.
- با کاربر `root` لاگین کرده و رمز عبور را وارد کنید.

> اگر اتصال SSH فعال نیست، ابتدا باید از طریق کنسول مستقیم آن را فعال کنید.

### 2. فعال‌سازی دسترسی Shell
```shell
Shell.set --enabled true
shell
