# 🔐 بازیابی رمز عبور حساب Administrator در vCenter

در صورتی که رمز عبور اکانت **Administrator@vsphere.local** را در vCenter فراموش کرده‌اید، نگران نباشید! اگر به کنسول اصلی vCenter دسترسی داشته باشید و رمز عبور کاربر `root` را بدانید، می‌توانید به‌راحتی رمز عبور را ریست کرده و دسترسی خود را بازیابی کنید.

برای انجام این کار مراحل زیر را دنبال کنید:

1. ابتدا نرم‌افزار [PuTTY](https://www.putty.org) را دانلود و نصب کنید. سپس آن را اجرا کرده و در قسمت **Host Name (or IP address)**، آدرس IP یا نام میزبان vCenter را وارد کنید.

2. با کاربر `root` وارد شوید و رمز عبور مربوطه را وارد نمایید. اگر SSH فعال نیست، از طریق کنسول مستقیم vCenter آن را با دستور زیر فعال کنید:

   ```bash
   Shell.set --enabled true
   shell
   ```

3. سپس ابزار مدیریتی زیر را اجرا کنید:

   ```bash
   /usr/lib/vmware-vmdir/bin/vdcadmintool
   ```

4. پس از اجرای ابزار، عدد `3` را برای گزینه Reset account password وارد کنید.

5. در مرحله بعد، نام کامل کاربری که قصد ریست کردن آن را دارید وارد نمایید. به‌طور پیش‌فرض:

   ```
   Administrator@vsphere.local
   ```

6. پس از زدن Enter، یک رمز عبور جدید برای شما تولید و نمایش داده خواهد شد.

اکنون می‌توانید با رمز جدید وارد محیط vCenter شوید و اقدامات مدیریتی لازم را انجام دهید.

**نکته امنیتی:** پس از ورود، حتماً رمز عبور را به رمز دلخواه خود تغییر دهید و مطمئن شوید که دسترسی به حساب `root` محدود و امن باقی بماند.

> این روش در نسخه‌های vCenter 6.7 به بعد تست‌شده و قابل‌استفاده می‌باشد.
